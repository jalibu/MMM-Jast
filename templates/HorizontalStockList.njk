{% import "templates/StockList.njk" as stockList with context %}
{% macro render(num="1") %}
  <span class="jast-hticker jast-ticker-no{{ num }}" >
    <ul class="jast-tickerframe">
    {% for i in range(0, config.virtualHorizontalMultiplier) -%}
      {{ stockList.render() }}
    {% endfor %}
    </ul>
  </span>
{% endmacro %}
